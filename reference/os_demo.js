const os = require('os');

// Platform
console.log(os.platform());

//CPU Arch
console.log(os.arch());

//CPU Core Info
//console.log(os.cpus());

//home directory
console.log(os.homedir());

// Uptime or second the system is been up
console.log(os.uptime());